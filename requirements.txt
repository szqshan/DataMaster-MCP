# DataMaster MCP 依赖包
# 核心MCP框架
mcp>=1.0.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# Excel支持
openpyxl>=3.1.0
xlrd>=2.0.0

# 注意：以下是Python内置模块，无需安装
# sqlite3, logging, datetime, pathlib, json, os, typing

# 数据分析
scipy>=1.10.0      # 科学计算和统计

# 环境变量管理
python-dotenv>=1.0.0

# 数据库连接支持（可选安装）
# MySQL支持
pymysql>=1.1.0

# PostgreSQL支持
psycopg2-binary>=2.9.0

# MongoDB支持
pymongo>=4.5.0

# API连接器依赖
requests>=2.28.0
xmltodict>=0.13.0  # 可选，用于XML解析

# 可选：未来扩展用
# requests>=2.31.0   # API调用